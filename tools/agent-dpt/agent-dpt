#!/usr/bin/env bash
# Repo-local agent-dpt wrapper.
# Keeps the root tool entry point stable while the implementation lives in bin/.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/bin/agent-dpt" "$@"
